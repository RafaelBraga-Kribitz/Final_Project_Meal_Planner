<form method="post" action="{{ path('app_meal_planner_delete', {'id': meal_planner.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meal_planner.id) }}">
    <button class="btn">Delete</button>
</form>
